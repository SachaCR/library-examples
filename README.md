> [!WARNING]
> **This repository is archived.** The example now lives in the Ontologic monorepo, at
> [`packages/library-example`](https://github.com/SachaCR/ontologic/tree/main/packages/library-example).
>
> Development continues there, where it builds against the library in the same workspace
> rather than a published release. The code here is kept only so that links in existing
> articles keep resolving; it is pinned to `ontologic@^1.6.2` and will not be updated.

# Library domain example with Ontologic

This repository is for teaching purposes. It illustrates how we can use the [Ontologic](https://ontologic.site) NPM package to model a domain.

## Where things went

| Here | Now |
| --- | --- |
| `src/` | [`packages/library-example/src/`](https://github.com/SachaCR/ontologic/tree/main/packages/library-example/src) |
| `docs/articles/` | [`packages/library-example/docs/articles/`](https://github.com/SachaCR/ontologic/tree/main/packages/library-example/docs/articles) |

The documentation for Ontologic itself is at [ontologic.site](https://ontologic.site).
